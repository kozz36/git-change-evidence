# Prepare v0.1.0-preview.1

## Scope and authority

Prepare the `git-change-evidence` `v0.1.0-preview.1` release candidate without publishing it.

Owner decisions:

- Preparation includes documentation and ephemeral release candidates.
- Candidate contents are source plus Linux binaries for `amd64` and `arm64`.
- Each candidate set includes SHA-256 checksums and an SBOM.
- Public repository visibility is planned, but no visibility change is authorized here.

This work MUST NOT push, open a pull request, create or publish a tag or release, change repository visibility, upload assets, publish a package or module, delete a branch, or perform any other publication action.

## Tasks

- [x] 1. **Apply the authorized repository hygiene.** Stop tracking generated `.atl/skill-registry.md`, add `.atl/` to `.gitignore`, and remove only `openspec/changes/.gitkeep`, `openspec/changes/archive/.gitkeep`, and `openspec/specs/.gitkeep`. Keep `.codegraph/` ignored and leave the intentional public root Go layout unchanged. Do not rewrite preserved OpenSpec history. **Evidence:** Before inventory found all four targets tracked; scoped name-status now shows exactly those four deletions plus `.gitignore` modified. `git check-ignore -v` matched `.codegraph/` at line 1 and `.atl/` at line 2. The sorted 72-file root Go manifest remained SHA-256 `3ec7b66b11e5cac98c685462c1dbcec000e3767e2c2dd0dec63f822d0c7160e1`; scoped `git diff --check` passed. **Recovery:** restore only this task's four removed tracked files and `.gitignore` change, then rerun the inventory.
- [ ] 2. **Canonically archive the completed `go-ast-census` change only.** Run canonical `sdd-archive` for `go-ast-census`, preserving every historical artifact and validating its delta spec, collisions, task completion, and archive destination. Do not manually move the change and do not archive any other change. **Depends on:** task 1. **Evidence:** **BLOCKED.** Two canonical executor attempts each ended after 1 turn with 0 tool calls and 0 writes, without running the required native command. A separate fresh native STATUS v2 call succeeded and reported 9/9 tasks complete, archive ready, no blockers or collisions, and `nextRecommended: archive`. No manual fallback was used; the authorized retry budget is exhausted. Task 2 remains pending and archive completion must not be claimed. **Recovery:** preserve the active change and this evidence; retry only under separate future authorization through the canonical archive path, never by manually moving or deleting evidence.
- [x] 3. **Record `bootstrap-neutral-core-extraction` as active and resumable.** Record the fresh native state—3 of 12 tasks complete, 9 pending, no verify report, and `nextRecommended: apply`—as an explicit lifecycle decision. Keep the change active; never archive, delete, rewrite, or represent it as complete during release preparation. **Depends on:** task 1 plus the documented task-2 outcome; task-2 completion is not required for this recording work. **Evidence:** Fresh native STATUS v2 reports 3/12 tasks complete, 9 pending, verify report missing, apply state `ready`, and `nextRecommended: apply`, with no relationships, same-domain collisions, blocked reasons, or notes. This task document is the current lifecycle decision location: the change remains active and resumable and MUST NOT be archived, deleted, rewritten, or represented as complete. **Recovery:** refresh read-only status if state changes; preserve the existing change bytes and resume through canonical `apply` only under separate authorization.
- [x] 4. **Pin the release baseline and audit candidate tooling.** Bind preparation to `origin/main@16aaa4498f91c6d1ff91b330e1df6a47db26c922` and record commit/tree identities, clean scoped state, merged PRs #98–#103, tags/releases, module/version sources, supported Go/tool versions, and available cross-build, archive, SHA-256, and SBOM tooling. Record unavailable tools without installing or configuring publication tooling. **Depends on:** tasks 1 and 3; the documented pending task 2 is not a hard dependency. **Evidence:** Read-only census (no fetch/ref mutation) recorded `git branch --show-current` = `docs/prepare-v0.1.0-preview.1`; `git rev-parse HEAD`, `HEAD^{tree}`, `origin/main`, and `origin/main^{tree}` = `16aaa4498f91c6d1ff91b330e1df6a47db26c922`, `e1926f0fafd43e56e0c719cd23afe0a1bf96fb33`, `16aaa4498f91c6d1ff91b330e1df6a47db26c922`, and `e1926f0fafd43e56e0c719cd23afe0a1bf96fb33`; `git merge-base HEAD origin/main` = the same commit; both `git merge-base --is-ancestor origin/main HEAD` and `git merge-base --is-ancestor HEAD origin/main` exited 0; and `git rev-list --left-right --count origin/main...HEAD` = `0\t0` (ahead/behind). `git status --short` was not clean: pre-existing `.atl/skill-registry.md` and `.gitignore` modifications, the three task-1 OpenSpec `.gitkeep` deletions, and untracked `odd/tasks/prepare-v0.1.0-preview.1.md`; `git diff --name-status` listed only those five tracked paths. The host-regenerated `.atl/skill-registry.md` was reported for parent restoration and was not attributed to the census. Read-only `gh pr view {98..103} --json number,state,mergedAt,mergeCommit,url` found all merged: #98 `916fb4943694d4e5e22b3ef5ea9bdd6d0286164c` (2026-09-17T20:25:46Z), #99 `37aa4150c053c4bbe0219db5cf9511fd68163aa4` (2026-09-17T20:36:49Z), #100 `32e1e47539ff93faac059a05523208b22a960eb6` (2026-09-17T20:39:48Z), #101 `de35e515594a34b6f93500d4f5c924454dfa065d` (2026-09-17T20:58:06Z), #102 `3e634d2688cd73865dc8fb0113c06a394cc271f4` (2026-09-17T21:08:18Z), and #103 `16aaa4498f91c6d1ff91b330e1df6a47db26c922` (2026-09-17T21:11:30Z). `git tag --list`, `git ls-remote --tags origin`, and `gh release list` each returned no entries. `go.mod` records module `github.com/kozz36/git-change-evidence` and `go 1.25.10`; `cmd/git-change-evidence/main.go` sets `previewVersion = "v0.1.0-preview.1"` and `--version` source output to `git-change-evidence v0.1.0-preview.1 (source/dev build; not release provenance)`. README and AGENTS both state this is an evidence-only planned source/dev preview, not a published tag/release/package/asset, and retain the canonical `gce census go-ast`, legacy `git-change-evidence census-go-ast`, root Go API, and `census` subpackage surfaces. `go env GOVERSION GOOS GOARCH` = `go1.27.1-X:nodwarf5`, `linux`, `amd64`; `go version` = `go version go1.27.1-X:nodwarf5 linux/amd64`; `go tool dist list | grep -xE 'linux/(amd64|arm64)'` returned both targets. Available tooling: `tar` GNU tar 1.35, `gzip` 1.14-modified, `sha256sum` GNU coreutils 9.11, and `file` 5.48 with its host magic database available. Unavailable (checked with `command -v`, not installed): `syft`, `cyclonedx-gomod`, `bom`, and alternatives `trivy`, `cdxgen`, `sbom-tool`, `spdx-sbom-generator`, `grype`, `tern`, and `cve-bin-tool`. Candidate manifest shape (definition only; no assets built): `git-change-evidence_v0.1.0-preview.1.manifest.json` records repository URL, ref `origin/main`, commit/tree above, version, Go/tool metadata, and one artifact object per deterministic filename: `git-change-evidence_v0.1.0-preview.1_source.tar.gz`; `git-change-evidence_v0.1.0-preview.1_linux_amd64.tar.gz`; `git-change-evidence_v0.1.0-preview.1_linux_arm64.tar.gz`; `git-change-evidence_v0.1.0-preview.1.sbom.spdx.json`; and `SHA256SUMS`. Each object records byte size and SHA-256 (including `SHA256SUMS` in the manifest; `SHA256SUMS` itself deterministically lists the source archive, both Linux archives, and SBOM in bytewise filename order), plus source member prefix `git-change-evidence-v0.1.0-preview.1/`, source commit/tree/version, and tool metadata. Linux objects record `GOOS=linux`, their respective `GOARCH`, `file` identity, and exactly archive members `gce` and `git-change-evidence`. The SBOM object records SPDX JSON format, its generator name/version or explicit unavailable status, the pinned source commit/tree/version, its own size/hash, and links to every source/binary artifact by filename, SHA-256, and architecture; the manifest records the same linkage. **Recovery:** resume from the recorded census and repeat read-only checks if refs, source, or tools changed.
- [x] 5. **Add the changelog and release-candidate runbook.** Create `CHANGELOG.md` and an English release-candidate runbook for `v0.1.0-preview.1` that binds preparation to the pinned inputs, distinguishes source/dev identity from release provenance, documents supported CLI and Go API surfaces, compatibility, limitations, verification, owner stops, and the active bootstrap change. Never claim that a tag, release, package, public repository, or downloadable asset exists. **Depends on:** task 4. **Evidence:** `CHANGELOG.md` and `docs/release-candidate-v0.1.0-preview.1.md` were created. Local Markdown-link resolution, required version/baseline/tree/binary/SBOM wording, portable-path review, publication-language review, and trailing-whitespace checks passed; `git diff --check` passed. Changed-line census after independent-review corrections: `CHANGELOG.md` +37/-0; `docs/release-candidate-v0.1.0-preview.1.md` +178/-0; this task-5 checkbox/evidence update +1/-1. **Recovery:** revert only these preparation documents or resume their checklist; do not alter product contracts to repair prose.
- [x] 6. **Build and delete ephemeral Linux candidate assets.** Outside the repository, create source material and Linux `amd64` and `arm64` candidate archives, with each Linux archive containing both `gce` and `git-change-evidence`; generate SHA-256 checksums and an SBOM. Validate archive members, architectures, hashes, SBOM coverage, version/help output and canonical/legacy parity where executable, record the evidence, then delete all temporary candidate output. Never copy or upload generated assets into the repository or elsewhere. **Depends on:** tasks 4–5. **Evidence:** A fail-fast, owner-only temporary rehearsal used an isolated Git index/object directory and pinned baseline `16aaa4498f91c6d1ff91b330e1df6a47db26c922` / `e1926f0fafd43e56e0c719cd23afe0a1bf96fb33`; the authorized pre-evidence working state produced rehearsal tree `846d84b88104b7ed8681e7d9aae9caa4d8e85cd3`. Current-module and extracted-tree Go inputs matched across 99 files (aggregate SHA-256 `3c5ef09cd815b13b181bdda828fdf04e8f40bf946690aa8232e9cecb668bfe23`). Go `go1.27.1-X:nodwarf5` built static Linux `x86-64` and `ARM aarch64` binaries with deterministic archive/build settings with byte-identical `gce`/`git-change-evidence` aliases and exact archive members. Temporary `cyclonedx-gomod@v1.10.0` `bin -json -noserial -notimestamp -version v0.1.0-preview.1` generated one unmodified CycloneDX 1.6 SBOM from each target's exact `gce` binary; each recorded the module/version, one component, two dependencies, no timestamp or local path. License collection was not requested because binary mode cannot download the unpublished main module. Five candidate files passed `SHA256SUMS`; the manifest and checksum file were also independently hashed. `amd64` exact version, root-help alias parity, canonical/legacy census-help parity, and 723-byte census JSON parity passed with empty stderr; `arm64` runtime was N/A without a runner, while architecture/build metadata passed. Full filenames, sizes, hashes, SBOM linkage/limitation, and check hashes are recorded in `docs/release-candidate-v0.1.0-preview.1.md`. The real index remained SHA-256 `278dfbbc15c241079b44f9ef373b65da65483b05d96e2d600e21fe316e5a4cb7` / Git blob `ffe6fd319c2bb5a6e2875e6e41e2fd16c0846c75`; `go.mod`/`go.sum` remained unchanged. The bounded cleanup trap deleted the entire temporary directory, tool, caches, isolated Git state, and all assets; absence and a zero repository asset scan passed. No asset was copied or uploaded. These were discarded rehearsal outputs, not release provenance. **Recovery:** discard the complete temporary directory and rebuild from pinned inputs; never reuse a partial candidate.
- [ ] 7. **Verify, review, and create two local conventional commits.** Run repository tests, race tests, configured check-only formatting, documentation links, license/version checks, and the release-candidate checklist; independently review the complete preparation diff and evidence; confirm no generated asset remains in the repository. Mark only observed completed work complete and keep task 2 explicitly pending with its BLOCKED evidence. First create one local conventional work-unit commit for the prepared diff without claiming all seven tasks or the archive are complete. Then create one local conventional evidence-ledger commit that records the work-unit commit identity in this task document. Both commits remain local. Do not push or open a pull request. **Depends on:** task 1 and tasks 3–6; task 2 is explicitly not a hard dependency. **Evidence:** _Pending: verification outputs, independent-review outcome, changed paths/lines, work-unit commit identity, evidence-ledger commit identity, clean post-commit status, explicit pending task-2 disclosure, and confirmation that no publication action occurred._ **Recovery:** before the work-unit commit, correct the working copy and rerun checks; after either commit, use a new local corrective commit unless the owner separately authorizes history rewriting.
