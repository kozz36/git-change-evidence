# Public Preview Readiness Tasks

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | 712–972 additions + deletions for the four implementation PRs, plus 765 observed planning-artifact lines in PR 0 |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 0 → PR 1 → PR 2 → PR 3 → PR 4 (one local planning PR plus four implementation PRs) |
| Delivery strategy | ask-on-risk (owner-resolved to a five-PR local stack) |
| Chain strategy | stacked-to-main |

### Itemized estimate

| Category | Concrete reviewer surface | Estimated changed lines |
|----------|---------------------------|------------------------|
| Production | `cmd/git-change-evidence/main.go` normalization, discovery, and source/dev version shell behavior | 50–80 |
| Tests | `cmd/git-change-evidence/main_test.go` and `cmd/git-change-evidence/census_test.go` | 150–220 |
| Mechanical | `AGENTS.md`, `openspec/config.yaml`, and narrow active-spec reconciliation | 60–100 |
| Artifacts | Complete root `LICENSE` Apache-2.0 text | 202 |
| Artifacts | `README.md` and `docs/census.md` | 80–120 |
| Artifacts | `docs/product-charter.md`, `docs/agent-consumers.md`, and `openspec/changes/public-preview-readiness/publication-audit.md` | 170–250 |
| **Artifacts subtotal** | **License, documentation, and audit records** | **452–572** |
| **Total** | **Production + tests + mechanical + artifacts** | **712–972** |

This forecast counts Apache `LICENSE` and documentation as reviewer surface even though they are not production code. The implementation forecast omitted 765 already-authored OpenSpec planning lines; combining those artifacts with the observed code slice would create an approximately 1,049-line review surface. The owner therefore selected a local PR 0 for planning artifacts before the four implementation PRs. This is a delivery correction, not a `size:exception`, and it grants no push authority.

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

The owner resolved `ask-on-risk` to one local planning PR followed by four implementation PRs. No `size:exception` is selected or authorized; keep every selected PR within the 400-line review budget or return any overage to the parent rather than altering this chain.

## Selected Stacked-PR Work-Unit Boundaries

The selected chain is PR 0 → PR 1 → PR 2 → PR 3 → PR 4: PR 0 planning artifacts target `main`; PR 1 CLI targets PR 0; PR 2 public entrypoint/license targets PR 1; PR 3 governance/consumer guidance targets PR 2; PR 4 audit/spec reconciliation targets PR 3. This records local delivery structure only; it does not create branches, commits, pushes, or PRs. Keep tests and the documentation they make true in the same unit; do not compress code, tests, legal text, or prose to meet the budget.

| PR / target | Start and finish boundary | Depends on | Verification and rollback boundary |
|-------------|---------------------------|------------|------------------------------------|
| PR 0 → `main`: Planning artifacts | Contains the owner-designated local OpenSpec planning-artifact set for `public-preview-readiness`, without implementation code. | Owner delivery correction. | Review the planning model and preserved phase history as one bounded local unit; no push is authorized. |
| PR 1 → PR 0: CLI compatibility and discovery | Starts with existing legacy-only dispatch; finishes with one source package accepting canonical and legacy census forms plus explicit discovery. | PR 0 and threshold review prerequisite. | Focused Go RED/GREEN/TRIANGULATE/REFACTOR checks plus two named-binary smoke checks; measure every changed existing Go file and stop for configured package-boundary review before any exceeds 160 changed lines; revert only `main.go` and its paired tests to restore legacy-only behavior. |
| PR 2 → PR 1: Public command entrypoint and license | Starts after tested command behavior; finishes with Apache-2.0 and accurate source-build/census instructions. | PR 1. | Review exact license text, links, examples, and both executable names; revert only `LICENSE`, `README.md`, and `docs/census.md` without changing runtime behavior. |
| PR 3 → PR 2: Governance and consumer guidance | Starts with truthful command wording from PRs 1–2; finishes with current-vs-historical governance reconciliation and a distinct consumer guide. | PR 2. | Review evidence-only language, English/portable prose, and retained deferrals; revert only `AGENTS.md`, `openspec/config.yaml`, `docs/product-charter.md`, and `docs/agent-consumers.md`. |
| PR 4 → PR 3: Publication audit and active-spec reconciliation | Starts from the completed current-facing material; finishes with classified audit coverage and only justified active-spec wording corrections. | PRs 2–3. | Review audit evidence versus uninspected surfaces and active-spec semantic invariance; preserve `publication-audit.md` as audit history, appending corrections or supersession records rather than reverting/removing it; revert only targeted active-spec prose, never archive/history artifacts. |

## Tasks

- [ ] 1. **Record apply prerequisites without changing delivery authority.** Before modifying implementation files, measure the actual branch-local additions plus deletions (including license, documentation, tests, mechanical edits, and new audit artifacts) against the 400-line budget for its selected stacked-PR boundary; no `size:exception` is authorized, so return any unit that cannot stay within budget to the parent rather than changing the selected chain. Inspect `openspec/config.yaml` threshold baselines and existing `apply-progress.md` records, if any: the CLI already has seven Go files against `cli_go_files: 6`, while this design must add no Go files. Before editing and after each work unit, measure additions plus deletions for every changed existing Go file—at minimum `cmd/git-change-evidence/main.go`, `cmd/git-change-evidence/main_test.go`, and `cmd/git-change-evidence/census_test.go`—against `changed_lines_per_go_file: 160`. Stop the affected work unit before an existing Go file exceeds 160 changed lines, record the crossing in the applicable `apply-progress.md`, and obtain the configured explicit package-boundary review before continuing that file; do not move helpers or tests into a new file to disguise either the per-file limit or the existing file-count crossing. **Depends on:** PR 0 planning artifacts recorded as the local base for PR 1. **Finish:** delivery-size facts and threshold/package-boundary prerequisites are explicit; no publication, visibility, tag, release, deletion, or external action occurs. **Rollback:** remove only a newly created progress note if its underlying implementation is abandoned; preserve historical records.

- [ ] 2. **RED — specify canonical normalization and side-effect-free discovery failures first.** In `cmd/git-change-evidence/main_test.go` and `cmd/git-change-evidence/census_test.go`, add failing, table-driven tests before production edits for: `normalizeCensusArgs` exact-prefix rewrite at empty, malformed, three-token, and valid tails; idempotence; unchanged legacy/unrelated inputs; preserved raw bytes and `--`; and nonmutation of the caller backing array. Add paired `runCommand` and `runCLI` tests proving `census go-ast`, `census go-ast --invalid`, and `census go-ast <new-file>` reach census validation with empty stdout, `invalid input\n`, exit 2, and no CWD, Git, stdin, publisher, or source-root access. Add failing tests for every exact discovery vector (`--help`, `census --help`, canonical and legacy census help, `--version`), terminal-LF stdout, empty stderr, exit 0, no effects, negative discovery vectors, and short/failing discovery writes returning exit 3 with `content unavailable\n`. Run the focused RED command and retain its expected failure evidence before editing `main.go`:
  ```sh
  go test ./cmd/git-change-evidence -run 'Test.*(Census|Discovery|Version|Normalize|RunCommand|RunCLI)'
  ```
  **Depends on:** task 1. **Finish:** new tests fail only because the specified compatibility/discovery behavior is absent. **Rollback:** remove only these unimplemented tests if the approved behavior is withdrawn.

- [ ] 3. **GREEN — implement the thin shared shell boundary in the existing CLI package.** In `cmd/git-change-evidence/main.go`, add the CLI-local `previewVersion` constant, pure `normalizeCensusArgs(args []string) []string`, and pure exact-vector discovery helper/writer. Apply discovery before normalization and normalization before `isCensusShaped` in both `runCommand` and `runCLI`; retain `parseCensusConfig` and `runCensusWith` grammar unchanged. Normalize only `census go-ast` into legacy `census-go-ast` with an allocated slice, preserve all tail tokens byte-for-byte, and reserve no broader `census` namespace. Make discovery write only human text plus a final LF to stdout; keep stderr empty and return 0 on success, or exit 3 plus the existing generic diagnostic when its output write fails. Version output must be exactly `git-change-evidence v0.1.0-preview.1 (source/dev build; not release provenance)`. Do not add `cmd/gce`, a wrapper, an alias requirement, a framework, schemas, configuration parsing, filesystem/Git/network effects, or a release mode. Run the focused command from task 2 until it passes. **Depends on:** task 2 RED evidence. **Finish:** the smallest implementation makes the RED cases pass without changing normal legacy output, schema, diagnostic, or exit contracts. **Rollback:** revert only `cmd/git-change-evidence/main.go` with the paired tests from tasks 2–5.

- [ ] 4. **TRIANGULATE — prove parity, boundaries, and the two output names.** Extend the same co-located tests with paired canonical/legacy valid census cases (matches, zero matches, reversed paths, Unicode identifiers, and non-UTF-8 paths) requiring byte-identical stdout, stderr, and exit status; pair invalid flags, missing/duplicate options, separator/path/identifier errors, unavailable source, parser failure, resource bounds, and output-writer failure. Preserve the current Linux hostile-file coverage and unsupported-platform behavior without claiming cross-compiled execution. Prove unrelated three-argument positional behavior remains unchanged for `project`, `publish`, and `census <source-other-than-go-ast> <new>`, and prove an immutable base ID remains usable for a source named `go-ast`. Add a temporary-directory executable smoke test or documented equivalent that builds and invokes both names from the one package for help/version and canonical/legacy census success/failure:
  ```sh
  tmpdir="$(mktemp -d)" && \
    go build -o "$tmpdir/gce" ./cmd/git-change-evidence && \
    go build -o "$tmpdir/git-change-evidence" ./cmd/git-change-evidence
  ```
  Rerun the focused command after each boundary case. **Depends on:** task 3 GREEN evidence. **Finish:** canonical and legacy paths have byte/status parity, discovery cannot contaminate evidence streams, and both real local output names work from the one source package. **Rollback:** revert these parity/smoke tests and the corresponding shell behavior together.

- [ ] 5. **REFACTOR — keep the CLI change small and establish Go checks.** Refactor only after GREEN/TRIANGULATE tests are green, retaining pure helpers in `cmd/git-change-evidence/main.go` unless a threshold review explicitly permits another package/file. Before and after refactoring, measure additions plus deletions for each changed existing Go file; stop before `main.go`, `main_test.go`, or `census_test.go` exceeds `changed_lines_per_go_file: 160`, record the crossing in `apply-progress.md`, and obtain the configured explicit package-boundary review before continuing changes to that file. Rerun the focused command after refactoring, then run the configured primary and check-only formatting commands exactly:
  ```sh
  go test ./...
  test -z "$(find . -path './.git' -prune -o -path './.codegraph' -prune -o -type f -name '*.go' -print0 | xargs -0 -r gofmt -l)"
  ```
  This planned behavior is not concurrency-bearing; if implementation introduces concurrent execution or synchronization, also run `go test -race ./...` and record its result. Confirm no golden, digest, schema, Go API, `internal/`, `go.mod`, workflow, or census-confinement change was introduced. **Depends on:** task 4 TRIANGULATE evidence. **Finish:** focused tests pass after refactor, configured applicable checks pass, and no existing Go file exceeds its threshold without the required stop and package-boundary review. **Rollback:** revert only the unit-1 CLI/test files, retaining all pre-existing contracts.

- [ ] 6. **Create the accurate public command entrypoint and Apache license.** Add root `LICENSE` as the complete, unmodified official Apache License 2.0 text including its appendix; compare it against the authoritative text during implementation, without inventing copyright, NOTICE, attribution, legal-clearance, or third-party-rights claims. Replace obsolete bootstrap assertions in `README.md` with concise English source/dev-preview guidance: evidence-only scope, planned `v0.1.0-preview.1` identity (not a published distribution), supported root Go API and public `census` subpackage, CLI/machine-contract primacy, the exact two `go build -o` commands from the design, and links to `AGENTS.md`, `docs/agent-consumers.md`, and `docs/census.md`. Update `docs/census.md` to lead with `./gce census go-ast`, retain the exact legacy invocation and equivalence, retain the Linux confinement/non-Linux-unavailable and supplied-scope limitations, and avoid claiming semantic resolution, whole-tree snapshotting, sandboxing, certification, approval, packaging, or release availability. Verify each example against the binaries built in task 4, inspect every README link/anchor, and manually compare license identity/text and all claims with implemented behavior. **Depends on:** task 5. **Finish:** current public instructions are executable and truthful; license/legal uncertainty is not overclaimed. **Rollback:** revert only `LICENSE`, `README.md`, and `docs/census.md`; do not remove rights already granted without owner/legal review.

- [ ] 7. **Reconcile current governance and author the separate consumer guide.** Update `AGENTS.md`, `openspec/config.yaml`, and `docs/product-charter.md` only to replace contradicted current-facing bootstrap statements with current facts: module/source/tests/CI exist; Apache-2.0 and the preview identity are selected; CLI-first positioning retains supported Go APIs; Go 1.25.10, Functional Core/Imperative Shell, layout, test commands, thresholds, evidence-only authority, and immutable CNSIC predecessor `a0fc7b26ff8a0e0a61baa586b32c46841611c806` remain. Preserve concrete configuration syntax, broad distribution, signing, and release automation as deferred; add a clear current-versus-historical boundary rather than rewriting bootstrap provenance. Create `docs/agent-consumers.md`, distinct from `AGENTS.md`, organized as consume → validate → reproduce → interpret. Cover canonical bytes/final LF/digest preservation, Base64 raw paths, revision-bound antecedents, census supplied-byte scope and non-atomic-tree limitation, stream/exit handling (including census 0/2/3/4 and documented existing command-specific exits), stale/incomplete/unavailable handling, independent regeneration, and consumer-owned delivery decisions. Verify all newly authored material is English and portable, every claim maps to code/specification, no material grants GCE delivery authority, and historical/local-path/literal fixtures remain unedited. **Depends on:** task 6. **Finish:** current governance and consumer guidance are accurate without erasing history or selecting excluded publication/distribution work. **Rollback:** revert only the four listed current-facing files, leaving historical OpenSpec and CNSIC provenance intact.

- [ ] 8. **Complete the bounded publication audit and narrow active-OpenSpec reconciliation.** Create `openspec/changes/public-preview-readiness/publication-audit.md` with per-surface rows for inspection method/scope or revision, observed fact, classification, coverage limitation, and owner follow-up. Classify current docs/examples, `.atl/skill-registry.md` (generated local-path metadata, not deletion authority), historical OpenSpec/CNSIC/local-path provenance, license/dependency/third-party questions, and any potential sensitive finding without reproducing secrets. Explicitly label Git history, unpublished refs, external settings/secrets/metadata, outside-tree generated or release assets, and legal/IP clearance as uninspected unless genuinely inspected under separate authority. Record owner-controlled decision points before any later visibility change, tag/release/asset publication, destructive remediation, credential action, or legal/confidentiality determination; these are informational consent boundaries, not GCE approvals or automated gates. Inspect active `openspec/specs/**/spec.md`; make only the justified prose-only update in `openspec/specs/inventory-v1-contract/spec.md` to distinguish its existing provenance sequence and WU-4C reference as historical while retaining the normative acquisition exclusion. Leave `openspec/specs/evidence-v1-provenance/spec.md` unchanged unless a concrete current-facing contradiction is found, and record the intentionally retained result. Do not modify `openspec/changes/archive/**`, prior validation/progress records, predecessor hashes, `.atl/skill-registry.md`, or literal historical evidence. **Depends on:** tasks 6–7. **Finish:** audit facts, benign context, actionable questions, and coverage gaps are distinguishable; only narrow active-spec wording changes occur. **Rollback:** preserve `publication-audit.md` as audit history even when implementation or findings change; append a dated correction, supersession, or withdrawal record instead of reverting or removing the audit, and revert only this task’s targeted active-spec prose. Preserve all history and audit trail.

- [ ] 9. **Perform the implementation-slice review for the selected stacked-PR sequence.** Recalculate additions plus deletions by unit and category, including the full Apache license, every documentation change, tests, mechanical reconciliation, and audit artifact; compare each selected PR with the 400-line budget without code-golfing or hiding formatting churn. Confirm each candidate unit still has its required tests/docs, focused command result, full `go test ./...` result, exact format-check result, applicable race-test result or recorded non-concurrency rationale, binary smoke result, documentation/license checklist, and independent rollback boundary. Recheck that no excluded work occurred: multilingual census, capabilities schema, evidence-schema/digest/API changes, internal/core policy changes, visibility change, tag/release/asset publication, history rewrite, deletion of `.atl` or archives, CNSIC cutover, MEH integration, packaging/signing/release automation, or delivery authority. **Depends on:** tasks 1–8. **Finish:** implementation evidence and actual review surface are ready for the selected stacked-PR reviews; no apply/verify/archive lifecycle claim, publication action, branch/commit/PR creation, or authority verdict is made by GCE. **Rollback:** use each unit’s independent rollback boundary rather than modifying historical artifacts or external state.
